<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chuyenctn.assignment">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="Quản lý thu chi"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Login_Activity" />
        <activity android:name=".activity.Dangki_Activity" />
        <activity
            android:name=".activity.Thongke_Activity"
            android:label="@string/title_activity_thongke_"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.Gioithieu_Activity"></activity>
    </application>

</manifest>